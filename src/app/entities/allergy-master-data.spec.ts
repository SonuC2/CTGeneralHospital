import { AllergyMasterData } from './allergy-master-data';

describe('AllergyMasterData', () => {
  it('should create an instance', () => {
    expect(new AllergyMasterData()).toBeTruthy();
  });
});
